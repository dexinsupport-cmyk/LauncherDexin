name: Dexin Actions Startup Test

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-22.04
    steps:
      - name: Actions runner test
        run: |
          echo "GitHub Actions runner started successfully"
          echo "SHA=$GITHUB_SHA"
